#include <stdio.h>
int main(){
int n,result;
int array[1]={2};
int array1[1]={3};

printf("the sum of the two arrays is =");

for (int i = 0 ; i<1 ; i++){
    result = array[i]+array1[i];
}
printf("%d",result);

    return 0;
}