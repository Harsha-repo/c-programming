#include<stdio.h>
#include<string.h>

int main(){

int pmatch(char *str,char *pat){

    int i=0,j=0;
    int lens =strlen(str);
    int lenp = strlen(pat);

    while(i<lens & j<lenp){

        i++;
        j++;
    }
}

    return;
}